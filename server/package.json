{
  "name": "suits-figma-mcp-server",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "MCP server and local bridge for creating and exporting Suits Workspaces Figma pitch deck frames.",
  "main": "src/index.ts",
  "scripts": {
    "start": "node src/index.ts",
    "start:http": "MCP_HTTP_MODE=true node src/index.ts",
    "dev": "node --watch src/index.ts"
  },
  "engines": {
    "node": ">=24"
  }
}
